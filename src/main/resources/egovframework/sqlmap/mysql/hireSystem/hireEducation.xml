<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hireEducationDao">

    <!-- 학력 목록 조회 (재학중 우선, 종료일 내림차순) -->
    <select id="selectEducationInfo" parameterType="int" resultType="hireEducationVo">
        SELECT
              EDUCATION_ID
            , RESUME_ID
            , SCHOOL_NAME
            , SCHOOL_TYPE
            , MAJOR
            , SUB_MAJOR
            , GRADE
            , GRADE_MAX
            , LOCATION
            , GRADUATE_TYPE
            , START_DATE
            , END_DATE
            , CURRENT_YN
        FROM HIRE_EDUCATION
        WHERE
            RESUME_ID = #{resumeId}
        ORDER BY
            CASE
                WHEN CURRENT_YN = 'Y' THEN 0
                ELSE 1
            END,
            END_DATE DESC
    </select>

    <!-- 학력 등록 -->
    <insert id="insertEducation" parameterType="hireEducationVo">
        INSERT INTO HIRE_EDUCATION (
              RESUME_ID
            , SCHOOL_NAME
            , SCHOOL_TYPE
            , MAJOR
            , SUB_MAJOR
            , GRADE
            , GRADE_MAX
            , LOCATION
            , GRADUATE_TYPE
            , START_DATE
            , END_DATE
            , CURRENT_YN
            , REG_DT
        ) VALUES (
              #{resumeId}
            , #{schoolName}
            , #{schoolType}
            , #{major}
            , #{subMajor}
            , #{grade}
            , #{gradeMax}
            , #{location}
            , #{graduateType}
            , #{startDate}
            , #{endDate}
            , #{currentYn}
            , NOW()
        )
        <selectKey keyProperty="educationId" resultType="int" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>

    <!-- 학력 수정 -->
    <update id="updateEducation" parameterType="hireEducationVo">
        UPDATE HIRE_EDUCATION
        <set>
            <if test="schoolName   != null">SCHOOL_NAME    = #{schoolName},</if>
            <if test="schoolType   != null">SCHOOL_TYPE    = #{schoolType},</if>
            <if test="major        != null">MAJOR          = #{major},</if>
            <if test="subMajor     != null">SUB_MAJOR      = #{subMajor},</if>
            <if test="grade        != null">GRADE          = #{grade},</if>
            <if test="gradeMax     != null">GRADE_MAX      = #{gradeMax},</if>
            <if test="location     != null">LOCATION       = #{location},</if>
            <if test="graduateType != null">GRADUATE_TYPE  = #{graduateType},</if>
            <if test="startDate    != null">START_DATE     = #{startDate},</if>
            <if test="endDate      != null">END_DATE       = #{endDate},</if>
            <if test="currentYn    != null">CURRENT_YN     = #{currentYn},</if>
            UPD_DT = NOW()
        </set>
        WHERE
            EDUCATION_ID = #{educationId}
        AND RESUME_ID    = #{resumeId}
    </update>

    <!-- 학력 삭제 -->
    <delete id="deleteEducation" parameterType="int">
        DELETE FROM HIRE_EDUCATION
        WHERE
            EDUCATION_ID = #{educationId}
    </delete>

</mapper>
