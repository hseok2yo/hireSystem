<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hireSignupDao">
	<!-- 아이디 중복체크 -->
	<select id="checkDuplicationID" parameterType="String" resultType="int">
		SELECT
			 COUNT(*)
		FROM HIRE_USER
		WHERE USER_ID = #{id}
	</select>

	<!-- 유저정보 등록 -->
	<insert id="insertUserInfo" parameterType="hireUserVo"
	useGeneratedKeys="true" keyProperty="userNum">
		INSERT INTO HIRE_USER (
			 USER_ID
			,USER_EMAIL
			,USER_PW
			,USER_NM
			,USER_PHONE
			,USER_MEMBER_TYPE
			,REG_DT
			,KAKAO_ID
			,LOGIN_TYPE
			,BIRTH_DATE
			)
		VALUES (
			 #{userId}
			,#{userEmail}
			,#{userPw}
			,#{userNm}
			,#{userPhone}
			,#{userMemberType}
			,NOW()
			,#{kakaoId}
			,#{loginType}
			,#{birthDate}
			)
<!-- 			<selectKey keyProperty="userNum" resultType="int" order="AFTER"> -->
<!-- 			SELECT LAST_INSERT_ID() -->
<!-- 		</selectKey> -->

	</insert>

	<!-- 약관동의 리스트 -->
	<select id="selectAgreeList" resultType="userAgreeDefVo">
		SELECT
			  AGREE_CD
			, AGREE_NM
			, REQUIRED_YN
			, USE_YN
			, REG_DT
		FROM USER_AGREE_DEF
	</select>

	<!-- 회원약관동의 정보 저장 -->
	<insert id="insertUserAgreement" parameterType="java.util.List">
		INSERT INTO USER_AGREEMENT (
			  USER_NUM
			, AGREE_CD
			, AGREE_YN
			, AGREE_DT
			, PAGES
		) VALUES
		<foreach collection="list" item="item" separator=",">
		(
			  #{item.userNum}
			, #{item.agreeCd}
			, #{item.agreeYn}
			, NOW()
			, #{item.pages}
		)
		</foreach>

	</insert>

	<!-- 회원정보 단건조회 -->
	<select id="selectHireUserInfo" parameterType="map" resultType="egovMap">
	     SELECT
	         h.USER_NUM
	       , h.USER_ID
	       , h.USER_EMAIL
	       , h.USER_PW
	       , h.USER_NM
	       , h.USER_PHONE
	       , h.USER_MEMBER_TYPE
	       , h.USER_PHOTO_ORIGINALNAME
	       , h.USER_PHOTO_NAME
		   , h.USER_PHOTO_PATH
	       , h.KAKAO_ID
	       , h.LOGIN_TYPE
	       , DATE_FORMAT(h.BIRTH_DATE, '%Y-%m-%d') AS BIRTH_DATE
	       , TIMESTAMPDIFF(YEAR, h.BIRTH_DATE, CURDATE()) AS AGE
	       , DATE_FORMAT(h.REG_DT, '%Y-%m-%d %H:%i:%s') AS REG_DT  -- 추가
	       , h.GENDER
	       , h.ADDRESS_FIRST
	       , h.ADDRESS_SECOND
	       , R.TITLE
	    FROM
	    	HIRE_USER h
	    LEFT JOIN RESUME R on
    		R.RESUME_ID = #{resumeId}
    		AND R.USER_NUM =h.USER_NUM
	    WHERE h.USER_NUM = #{userNum}
	</select>

	<!-- 회원정보 업뎃 -->
	<update id="updateBasicHireUserInfo" parameterType="hireUserVo">
		UPDATE HIRE_USER
	    <set>
	        USER_NM = #{userNm},
	        GENDER = #{gender},
	        BIRTH_DATE = #{birthDate},
	        USER_EMAIL = #{userEmail},
	        USER_PHONE = #{userPhone},
	        ADDRESS_FIRST = #{addressFirst},
	        ADDRESS_SECOND = #{addressSecond}
	    </set>
	    WHERE USER_NUM = #{userNum}
	</update>

</mapper>