<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">

    <!-- Appenders -->
    <Appenders>
        <!-- Console appender -->
        <Console name="console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%p] [%c] - %m%n" />
        </Console>

        <!-- File appender for logging SQL queries to a file -->
        <File name="file" fileName="logs/sql-queries.log">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%p] [%c] - %m%n" />
        </File>
    </Appenders>

    <!-- Loggers -->
    <Loggers>
        <!-- JDBC queries logging -->
        <Logger name="jdbc.sqlonly" level="DEBUG" additivity="false">
            <AppenderRef ref="console"/>
            <AppenderRef ref="file"/>
        </Logger>

        <Logger name="jdbc.sqltiming" level="DEBUG" additivity="false">
            <AppenderRef ref="console"/>
            <AppenderRef ref="file"/>
        </Logger>

        <!-- Log for SQL execution (with time info) -->
        <Logger name="jdbc.resultset" level="DEBUG" additivity="false">
            <AppenderRef ref="console"/>
            <AppenderRef ref="file"/>
        </Logger>

        <Logger name="jdbc.connection" level="DEBUG" additivity="false">
            <AppenderRef ref="console"/>
            <AppenderRef ref="file"/>
        </Logger>

        <!-- General logging -->
        <Root level="INFO">
            <AppenderRef ref="console"/>
        </Root>
    </Loggers>

</Configuration>
